<h1>Sample App</h1>
<%= form_for(@site, url: emojify_link_add_path(@site)) do |f| %>
  <%= f.label :url %>
  <%= f.url_field :url %>

  <%= f.label :blot %>
  <%= f.text_field :blot %>

  <%= f.submit "Emoji It", class: "btn btn-primary" %>
<% end %>
